{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the bundled frontend call this app's own commands. Nothing else is granted: the UI needs no filesystem, shell or network access, since every side effect goes through the MIDI link the backend owns.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
